📅 **작성일** : 2026-09-08

# 🌅 Today Learning Plan

## 🎯 오늘의 목표

- Entity를 DTO로 변환하는 팩토리 메서드를 작성하고 변환 흐름을 설명한다.
- 외부 DB와 연동하여 JPA CRUD 테스트를 실행하고 결과를 확인한다.
- JPA 연관관계 옵션을 구분하고 User Controller에 Validation을 적용한다.
- 인증과 인가의 역할과 차이를 설명한다.

---

## 📌 오늘 학습 계획

1. **Entity-DTO 변환**
    - Entity와 DTO의 역할을 구분한다.
    - Entity를 DTO로 변환하는 팩토리 메서드 패턴을 확인하고 작성한다.
2. **외부 DB 연동과 JPA CRUD 테스트**
    - Spring Boot와 외부 DB의 연동 설정을 확인한다.
    - JPA 기반 등록·조회·수정·삭제 JUnit 테스트를 작성하고 실행 결과를 확인한다.
3. **JPA 연관관계 옵션**
    - `cascade`, `orphanRemoval`, `fetch` 속성의 역할과 차이를 비교한다.
    - 각 옵션이 연관된 Entity의 저장·삭제·조회에 미치는 영향을 확인한다.
4. **User Controller Validation**
    - 사용자 요청값에 검증 조건을 적용한다.
    - 올바른 요청과 잘못된 요청의 처리 결과를 확인한다.
5. **인증과 인가**
    - 인증과 인가가 각각 무엇을 확인하는지 구분한다.
    - 사용자 요청 처리 과정에서 두 개념이 적용되는 순서를 정리한다.

---

## 🔄 복습할 부분

- 전날 TIL의 복습할 부분이 제공되지 않았습니다.

---

## ✅ 완료 기준

오늘이 끝났을 때 아래를 만족하면 성공이다.

- Entity를 DTO로 변환하는 팩토리 메서드를 작성하고 동작을 설명할 수 있다.
- 외부 DB 연동 상태에서 JPA CRUD JUnit 테스트를 실행하고 결과를 확인했다.
- `cascade`, `orphanRemoval`, `fetch`의 역할과 차이를 설명할 수 있다.
- User Controller에 Validation을 적용하고 요청 검증 결과를 확인했다.
- 인증과 인가의 차이를 구분하여 설명할 수 있다.

---

## 🤔 예상되는 어려움

- Entity와 DTO 사이의 변환 책임과 팩토리 메서드의 위치를 혼동할 수 있다.
- 외부 DB 설정과 JPA CRUD 테스트의 실행 결과를 연결하기 어려울 수 있다.
- `cascade`, `orphanRemoval`, `fetch`의 적용 목적과 동작 차이를 혼동할 수 있다.
- Validation이 적용되는 시점과 검증 실패 시 처리 흐름을 놓칠 수 있다.
- 인증과 인가의 역할을 비슷한 개념으로 혼동할 수 있다.

---

## 💡 해결 전략

- 변환 전후 객체를 비교하며 Entity에서 DTO로 데이터가 이동하는 흐름을 직접 설명한다.
- 외부 DB 연결 상태와 CRUD 테스트 실행 결과를 단계별로 확인한다.
- 각 연관관계 옵션을 저장·삭제·조회 관점으로 나누어 비교한다.
- 정상 요청과 검증 실패 요청을 각각 실행하여 처리 결과를 대조한다.
- 인증은 사용자 확인, 인가는 권한 확인으로 구분하여 요청 흐름에 적용해 본다.

---

## 📝 메모

- 별도 메모 없음.